# Processed Data

This folder contains cleaned and preprocessed datasets used for analysis and modelling.

The processed data has been:

- Filtered for relevant sampling methods
- Cleaned to remove duplicates and inconsistencies
- Standardised across multiple data sources
- Transformed into analysis-ready formats

These datasets are generated from the scripts in the `scripts/` folder.
